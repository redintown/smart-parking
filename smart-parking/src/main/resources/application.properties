# Server Configuration
#server.port=8080

# Database Configuration
# spring.datasource.url=jdbc:mysql://localhost:3306/smart_parking_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true&autoReconnect=true&failOverReadOnly=false&maxReconnects=10&initialTimeout=2
# spring.datasource.username=root
# spring.datasource.password=AllahvoroshA2113!!
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection Pool Configuration (HikariCP - default in Spring Boot)
# spring.datasource.hikari.connection-timeout=30000
# spring.datasource.hikari.maximum-pool-size=10
# spring.datasource.hikari.minimum-idle=5
# spring.datasource.hikari.idle-timeout=300000
# spring.datasource.hikari.max-lifetime=600000
# spring.datasource.hikari.leak-detection-threshold=60000

# JPA/Hibernate Configuration
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.open-in-view=false
# spring.jpa.properties.hibernate.connection.provider_disables_autocommit=false

# Static Resources Configuration
# spring.web.resources.static-locations=classpath:/static/
# spring.mvc.static-path-pattern=/**
# spring.web.resources.add-mappings=true

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Using H2 in-memory database (no setup required)
# Comment out H2 config and uncomment PostgreSQL/MySQL config below to use external database

# ============================================
# DATABASE CONFIGURATION - IMPORTANT!
# ============================================
# WARNING: H2 in-memory database (mem:) loses ALL data on restart!
# Use H2 ONLY for testing. For production with data persistence, use PostgreSQL/MySQL.

# H2 In-Memory Database (Data is LOST on restart - use for testing only)
spring.datasource.url=jdbc:h2:mem:smartparkingdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# PostgreSQL Configuration (Supabase) - UNCOMMENT TO USE PERSISTENT DATABASE
# IMPORTANT: If you want data to persist after restart, uncomment these lines:
# spring.datasource.url=jdbc:postgresql://db.mfrkafnbpirilcqchzjp.supabase.co:5432/postgres?sslmode=require
# spring.datasource.username=postgres
# spring.datasource.password=AllahvoroshA2113!!
# spring.datasource.driver-class-name=org.postgresql.Driver

# MySQL Configuration (Commented - Uncomment to use MySQL)
# spring.datasource.url=jdbc:mysql://localhost:3306/smart_parking_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true&autoReconnect=true&failOverReadOnly=false&maxReconnects=10&initialTimeout=2
# spring.datasource.username=root
# spring.datasource.password=AllahvoroshA2113!!
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Use H2 dialect for H2, PostgreSQL dialect for Supabase
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

